<preference-headers xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- These settings headers are only used on tablets. -->

    <header
        android:fragment="${packageName}.${activityClass}$GeneralPreferenceFragment"
        android:title="@string/pref_header_general" />
    <header
        android:fragment="${packageName}.${activityClass}$ScreenPreferenceFragment"
        android:title="@string/pref_header_screen" />
    <header
        android:fragment="${packageName}.${activityClass}$FontsPreferenceFragment"
        android:title="@string/pref_header_fonts" />

</preference-headers>